{
  "name": "@opsen/docker-compose",
  "version": "0.2.4",
  "description": "SSH-based Docker Compose deployer with MirrorState file sync",
  "main": "src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@pulumi/command": "^1.0.0",
    "@pulumi/pulumi": "^3.152.0",
    "ssh2": "^1.16.0"
  },
  "devDependencies": {
    "@opsen/testing": "workspace:^",
    "@tsconfig/node22": "^22.0.0",
    "@types/ssh2": "^1.15.5",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "main": "dist/index.js",
    "types": "dist/index.d.ts"
  }
}
