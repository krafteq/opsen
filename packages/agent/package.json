{
  "name": "@opsen/agent",
  "version": "0.4.1",
  "description": "Pulumi component for deploying the opsen deploy agent to VMs",
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "go",
    "src"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@opsen/docker-compose": "workspace:^",
    "@pulumi/command": "^1.0.0",
    "@pulumi/pulumi": "^3.152.0",
    "@pulumi/tls": "^5.0.0"
  },
  "devDependencies": {
    "@opsen/testing": "workspace:^",
    "@tsconfig/node22": "^22.0.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "main": "dist/index.js",
    "types": "dist/index.d.ts"
  }
}
