{
  "name": "@opsen/docker",
  "version": "0.1.2",
  "description": "Docker single-host runtime deployer with Caddy ingress",
  "main": "src/index.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@opsen/platform": "workspace:^",
    "@pulumi/docker": "^4.6.0",
    "@pulumi/pulumi": "^3.152.0"
  },
  "devDependencies": {
    "@opsen/testing": "workspace:^",
    "@tsconfig/node22": "^22.0.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "main": "dist/index.js",
    "types": "dist/index.d.ts"
  }
}
